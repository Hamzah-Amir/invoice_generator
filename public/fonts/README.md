# Custom fonts

Place any `.ttf` files in this directory to override the default Helvetica font that pdf-lib embeds while rendering invoices. The generator automatically loads the first `.ttf` file it finds at runtime and applies it to every drawn text block. Remove all files to fall back to Helvetica.

